FROM golang:1.20

WORKDIR /usr/src/di

# curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.3/install.sh | bash
# nvm install node
# npm install --global yarn

# docker build -t di-dev .
# docker run -it --name di-running-app -v di-dev:/app di-dev